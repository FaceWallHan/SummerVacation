<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.start.head">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:name=".AppClient"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity"/>
        <activity android:name=".activity.BroadcastStandardActivity" />
        <activity android:name=".activity.BroadcastActivity" />

        <receiver
            android:name=".broadcast.MyBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="100">

                <!-- 100为有序广播的优先级 -->
                <action android:name="com.start.head.broadcast.MY_BROADCAST" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".broadcast.BootCompleteReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- 接收开机广播 -->
            </intent-filter>
        </receiver>

        <activity android:name=".activity.FragmentNewsActivity" />
        <activity android:name=".activity.FragmentTestActivity" />
        <activity android:name=".activity.NewsContentActivity" />
        <activity android:name=".activity.RecyclerViewActivity" />
        <activity android:name=".activity.ListViewTestActivity" />
        <activity android:name=".activity.FirstActivity" />
        <activity android:name=".activity.ThirdActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Activity supporting ACTION_VIEW is not set as BROWSABLE -->
                <data android:scheme="http" />
                <!-- 指定http就可以和浏览器一样，能够响应打开网页的Intent了          /> -->
            </intent-filter>
        </activity>
        <activity android:name=".activity.LitePalActivity"/>
        <activity android:name=".activity.SecondActivity">
            <intent-filter>
                <action android:name="com.start.head.ACTION_START" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="com.start.head.MY_CATEGORY" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity"/>
        <activity
            android:name=".activity.PermissionActivity"
            android:label="This is FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>